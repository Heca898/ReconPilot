[build-system]
requires = []
build-backend = "packaging_backend"
backend-path = ["."]

[project]
name = "reconpilot"
version = "1.0.0"
description = "Deterministic reconnaissance and vulnerability-enrichment CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
reconpilot = "reconpilot.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
